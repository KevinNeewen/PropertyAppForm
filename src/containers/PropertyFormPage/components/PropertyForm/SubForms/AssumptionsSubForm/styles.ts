import { createStyles } from '@material-ui/styles';

const styles = (): any =>
    createStyles({
        root: {},
        subFormDetails: {
            width: '40%',
            height: '45vh',
        },
    });

export default styles;
