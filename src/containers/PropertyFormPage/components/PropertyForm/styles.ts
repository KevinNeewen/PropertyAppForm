import { createStyles } from '@material-ui/styles';

const styles = (): any =>
    createStyles({
        subFormContainer: {
            paddingTop: '10rem',
            height: '100vh',
        },
        subForm: {
            marginLeft: 0,
        },
        subFormActive: {
            marginLeft: 0,
            height: '100%',
        },
    });

export default styles;
